<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quiz Score</title>
    <link rel="stylesheet" href="/css/score.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
    <div class="container text-center mt-5">
        <h1>Quiz Results</h1>
        <%for(let d of data){%>
        <div class="score-card">
            <h2>Welcome, <%= d.userId%>!</h2>
            <p>Your total score in <%= d.Category %>is:</p>
            <h3><%= d.marks %> / 100</h3>
        </div>
        <%}%>
        <% if (data[0].userId) { %>
            <a href="/profile/<%= data[0].userId %>" class="btn btn-primary mt-3">Go to profile</a>
            <a href="/project/<%= data[0].userId %>" class="btn btn-secondary mt-3">Home Page</a>
            <p class="text-danger" style="font-size: 2em;">Click start again for answers</p>
        <% } else { %>
            <a href="/all/guest" class="btn btn-secondary mt-3">Take Quiz Again</a>
        <% } %>
    </div>
</body>
</html>
